<%= link_to "Create new Task", new_todo_path %>
<ul>
	<% @todos.each do |todo| %>
		<li>[<%= link_to todo.title, todo %>] 
			<%= todo.content %> 
			[<%= link_to "Edit", edit_todo_path(todo) %>]
			[<%= link_to "Delete", todo_path(todo), method: :delete %>]
		</li>
	<% end %>
</ul>
